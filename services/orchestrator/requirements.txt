# Runtime optional dependencies — all have stdlib fallbacks (urllib / asyncio)
# Install the full set for production performance:
#   pip install -r requirements.txt
aiohttp>=3.9,<4
aiofiles>=23.2,<24
requests>=2.31,<3

# Test / development dependencies
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5.0,<6
